/**
 * /app/landing — example nested route under the app layout.
 */
export function LandingPage() {
  return (
    <div className="app-panel">
      <h2>Landing</h2>
      <p>Example page under the shared /app shell. Replace with real content in later phases.</p>
    </div>
  )
}
